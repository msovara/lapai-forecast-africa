# Example: persist teacher forecast instead of printer sink.
# Copy to configs/inference_aifs_my_run.yaml and edit paths / CDS credentials.
#
# Checkpoint line is rewritten to an absolute path by scripts/run_aifs_inference.py :
#   python scripts/run_aifs_inference.py --template configs/inference_aifs_my_run.yaml

checkpoint: models/teacher/aifs-single-mse-1.0.ckpt

lead_time: 240

date: "2022-06-01T00"

input:
  dataset: test

# Persisting output (see Saving Outputs):
# https://anemoi.readthedocs.io/projects/inference/en/latest/usage/advanced/saving.html
# Typical NetCDF shape (adapt if your inference version uses a nested dict):
#
# output:
#   netcdf: data/processed/lapai/aifs_baseline_20220601.nc
#
# GRIB alternative:
# output:
#   grib: data/processed/lapai/aifs_baseline_20220601.grib

output:
  printer
